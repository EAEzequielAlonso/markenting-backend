export enum BookOwnershipType {
    CHURCH = 'CHURCH',
    MEMBER = 'MEMBER',
}

export enum BookStatus {
    AVAILABLE = 'AVAILABLE',
    LOANED = 'LOANED',
}

export enum LoanStatus {
    ACTIVE = 'ACTIVE',
    RETURNED = 'RETURNED',
}
