export enum BookOwnershipType {
    CHURCH = 'CHURCH',
    MEMBER = 'MEMBER',
}

export enum BookStatus {
    AVAILABLE = 'AVAILABLE',
    LOANED = 'LOANED',
}

export enum LoanStatus {
    REQUESTED = 'REQUESTED',
    ACTIVE = 'ACTIVE',
    RETURNED = 'RETURNED',
    REJECTED = 'REJECTED',
}
